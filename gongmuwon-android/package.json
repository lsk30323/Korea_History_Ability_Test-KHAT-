{
  "name": "gongmuwon-android",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare:web": "node prepare-web.mjs",
    "sync": "npm run prepare:web && cap sync android",
    "build:apk": "npm run sync && cd android && ./gradlew assembleDebug",
    "build:aab": "npm run sync && cd android && ./gradlew bundleRelease",
    "assets": "capacitor-assets generate --android --iconBackgroundColor #1e3a5f --iconBackgroundColorDark #10213a --splashBackgroundColor #1e3a5f --splashBackgroundColorDark #10213a",
    "open:android": "npm run prepare:web && cap open android",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5"
  }
}
